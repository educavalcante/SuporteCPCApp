import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        backgroundColor: '#1F1E25',
        borderRadius: 5,
        flexDirection: 'row',
        marginBottom: 10 //desgrudar um do outro
    },
    textCopyRight: {
        color: '#FFF',
        fontSize: 8
    }
})